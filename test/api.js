const {
  ipfsEncryptAndAdd,
  ipfsGetAndDecrypt,
  decryptBuffer,
  encryptString
} = require('../src/api')

describe("api", () => {

  // TODO: implement these tests - FOR NOW they are tested by usage in the 
  //       credentials.js smart contract test cases

  it("should encrypt a string")

  it("should decrypt an Encrypted record")

  it("should get and decrypt and encrypted record on IPFS")

  it("should encrypt and push data to IPFS")

})
